rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    
    // Public: Everyone can view images
    // Private: Only authenticated admins can upload or delete
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}